# Guessban - Greenfield Config
# Relaxed mode for early exploration and prototyping.
# Agent proceeds with assumptions but warns about them.

clarification_policy:
  allowed: true
  max_questions: 5
  stop_on:
    - conflicting_requirements
  default_if_silent: proceed

# Guessban - Minimal Config
# Good starting point for most projects.

clarification_policy:
  allowed: true
  max_questions: 2
  stop_on:
    - missing_data_schema
    - conflicting_requirements
  default_if_silent: warn

# Guessban - Active Policy
# https://github.com/engelrico/guessban

mode: pre_analyze  # oder: mid_impl, oder: both

stop_on:
  - missing_data_schema
  - undefined_auth_model

mid_impl:
  check_after: task   # neu: wann im impl-Prozess
  stop_on:
    - breaking_spec_change
    - new_missing_schema

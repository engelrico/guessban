# Guessban - Active Policy
# https://github.com/engelrico/guessban

clarification_policy:
  allowed: true
  max_questions: 2
  stop_on:
    - missing_data_schema
    - conflicting_requirements
  default_if_silent: warn
